New post with title "<%= @post.title %>" has been favorited.
<%= "=" * (@post.title.length + 12) %>

You will receive updates when commented on.

"View Post on site", topic_post_url(@post.topic, @post)